/**
 * footer.ts - Footer component for the Grazioso Salvare application
 * Displays the footer section with copyright information and links
 * Provides consistent navigation and branding across all pages
 */

import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  imports: [],
  templateUrl: './footer.html',
  styleUrl: './footer.scss',
})
export class Footer {
  // Currently a static component with no dynamic functionality
}
