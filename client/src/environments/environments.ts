/**
 * environments.ts - Environment configuration for the Angular application
 * Defines environment-specific variables used throughout the application.
 * This is the development configuration (production: false).
 * For production, we use the production environment file (environment.prod.ts).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api'
};
